{
  "name": "PDC Lab Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt update && sudo apt install -y libopenmpi-dev openmpi-bin && pip install mpi4py",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylance"
      ]
    }
  }
}